services:
  app:
    build:
      context: ./app
      dockerfile: Dockerfile
    ports: 
      - "3000:3000"
    env_file:
      - ./app/.env
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
  
